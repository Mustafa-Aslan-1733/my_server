[Unit]
Description=Weekly Docker image garbage collection

[Timer]
OnCalendar=weekly
RandomizedDelaySec=1h
# A run missed while the machine was down fires at the next boot instead of being skipped.
Persistent=true

[Install]
WantedBy=timers.target
